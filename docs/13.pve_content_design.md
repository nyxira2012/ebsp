# 13. PVE 探索副本内容与配置设计 (PVE Content & Configuration Design) v1.0

> **前置依赖**:
> - Doc 1 — 战斗设计（机体、驾驶员、武器、属性计算）
> - Doc 8 — 物品系统（装备 ilvl 与掉落逻辑）
> - Doc 10 — PVE 讨伐战系统设计（事件序列、快照机制）

---

## 1. 系统概述与核心概念

为了使 PVE 内容易于配置、扩展且具备足够的玩家吸引力，本设计文档将探索内容的具体落地细节进行规范化。

- **副本 (Instance)**：将原本的“大地图”、“大区域”统一称为“副本”。副本是玩家进行 PVE 探索的顶层容器，决定了背景主题、基础难度底线以及掉落产出。
- **子区域区分 (Zone Differentiation)**：
  - **常规子区域**：标准的探索路径，遵循该副本的基础掉落设定。
  - **隐藏子区域 (Hidden Zone)**：由概率触发进入。没有任何复杂的环境Debuff机制，其核心价值极度纯粹——**产出更高品质的物品**。在隐藏子区域中，掉落的物品品质等级（ilvl）固定 **+5**，且整体掉落概率显著提升。

---

## 2. 敌方生成与配置快照 (Enemy Configuration & Scaling)

为了避免为每个副本手工填无数的实体怪兽数据，敌方的生成采用“**组件拼装 + 动态缩放**”的机制。

### 2.1 拼装机制 (Component Assembly)
当生成一个敌人实例时，其数据来源于：
1. **基础机体模板**：从全局机体库中抽取符合该副本主题的特定机体。
2. **基础武器模板**：从全局武器库中抽取特定武器搭配。
3. **特制驾驶员**：副本会配置专属的敌方驾驶员模版（如：“废弃站防卫AI”、“精英海盗头目”），赋予特定的驾驶员属性（攻击、防守、反应等）。

### 2.2 属性缩放系数 (Scaling Factor)
在生成该敌人的**战斗快照 (MechaSnapshot)** 时，统一应用副本配置中该层级/该怪物的**属性缩放系数**。
- *例如：废弃空间站的某精英怪设定为 `hp_scale = 1.5`, `damage_scale = 1.2`。则其最终的快照 HP 就是基础机体 HP * 1.5。*
通过这一系数，策划可以简单地复用相同的机体和武器，通过调节系数来适配从新手副本到终局副本的数值梯度。

---

## 3. 序列配置规范 (Sequence Blueprint)

摒弃复杂的“岔路”和“迷宫”设计，副本内采取纯粹的**单线线性序列**。序列的具体构成由配置文件严格定义。

序列配置项需包含以下核心参数：
- **总长度**：本次探索序列的步数（例如 10 步）。
- **固定怪物位置**：序列中哪些具体索引位置是必须发生固定战斗的。
  - *例如：固定第 5 步为“精英怪（Elite）”，最后一步（第 10 步）永远为“Boss”。*
- **暗雷数量/分布**：在去除了固定位置和安全事件位置后，剩余的空白节点用于填充“暗雷（普通遭遇战）”。可以配置暗雷的总数量或每个格子的触发概率。

---

## 4. 全局随机事件池 (Global Event System)

考虑到开发维护成本和通用性，随机事件（事件节点）采用**全局抽象机制**，全副本共享同一个事件系统。

- **核心机制**：在非战斗或固定安排的事件节点触发。
- **第一阶段落地**：目前事件池中仅包含**补给类事件（恢复设定比例的 HP 和 EN）**。其核心作用是在消耗巨大的线性探索中，给玩家提供喘息和续航的节点。
- **可扩展性**：机制已抽离并做了抽象。后期可以无缝补充“商人”、“剧情对话”、“赌博盲盒”等全局共享事件，无需改动现有副本结构。

---

## 5. 掉落与区域特产设计 (Loot & Regional Specialties)

战斗与探索的核心驱动力是掉落。掉落配置直接绑定在副本的难度梯度与主题上。

### 5.1 区域特产机制 (Regional Core Materials)
- 设定特定的核心制造材料是**某些副本的特产**。
- *例如：想要制造某高级装甲，就必须去刷“废弃空间站”获取其独享掉落的“高纯度废旧合金”。*
- 这种机制鼓励玩家为了特定的养成目标去针对性地攻略特定副本，而不是永远只刷最高级副本。

### 5.2 稀有物品 Boss 独占掉落 (Boss Exclusive Rare Loot)
- 在通关结算/击杀 Boss 时，触发该副本专属的 `boss_loot_table`。
- 特定图纸、极品成装、特殊机体核心，**仅由具体的 Boss 以极低概率产出**。
- 此配置通过服务端配置文件精准控制（例如 `drop_rate: 0.005`），支撑长线的“刷宝”追求。

---

## 6. 配置示例 (配置文件结构蓝图)

以下是以 `abandoned_station` (废弃空间站) 和真实数据 ID 为例的配置数据蓝图，并附带了中文注释以便理解。

```json
{
  "instance_id": "abandoned_station",
  "name": "废弃空间站",
  "base_ilvl": 10,  // 基础掉落物品等级
  
  "zones": {
    "dock": {
      "name": "空间站码头",
      "type": "NORMAL", // 常规子区域
      "ilvl_bonus": 0,
      "drop_rate_mult": 1.0,
      "sequence": {
        "length": 10,   // 事件序列总长度
        "fixed_encounters": {
          "5": {"type": "elite", "pool": "station_elites"}, // 第5步固定遭遇精英怪池
          "10": {"type": "boss", "pool": "station_boss"}    // 第10步固定遭遇Boss池
        },
        "random_encounter_chance": 0.4, // 暗雷触发概率
        "total_random_encounters": 4,   // 当前子区域暗雷总次数上限
        "event_chance": 0.2             // 补给事件触发概率
      }
    },
    "warehouse": {
      "name": "空间站仓库",
      "type": "HIDDEN", // 隐藏子区域
      "spawn_chance": 0.15, // 隐藏区出现概率
      "ilvl_bonus": 5,      // 隐藏区高品质掉落加成
      "drop_rate_mult": 2.5,
      "sequence": {
        "length": 5,
        "fixed_encounters": {
          "5": {"type": "boss", "pool": "warehouse_guard"}
        }
      }
    }
  },

  "enemy_templates": {
    // 普通精英怪配置模板
    "station_elites": {
      "mecha_id": "mech_zaku",           // 装配机体：扎古II
      "weapon_ids": ["wpn_machine_gun"], // 装配武器：120mm机炮
      "pilot_id": "pilot_dummy",         // 驾驶员：木桩驾驶员 (作为普通自动防卫AI)
      "scaling": {                       // 难度缩放系数
        "hp_mult": 1.5,
        "damage_mult": 1.2
      }
    },
    // Boss配置模板
    "station_boss": {
      "mecha_id": "mech_rx78",           // 装配机体：RX-78 高达作为关底镇守
      "weapon_ids": ["wpn_beam_rifle", "wpn_beam_saber"],
      "pilot_id": "pilot_amuro",         // 驾驶员：阿姆罗
      "scaling": {                       // Boss级难度缩放
        "hp_mult": 4.0,
        "damage_mult": 1.5
      }
    }
  },

  "loot_tables": {
    "regional_specialty": ["mat_scrap_alloy", "mat_old_circuit"], // 区域特产材料掉落池
    "boss_drops": [
      {"item_id": "blueprint_rare_shield", "chance": 0.02} // Boss 专有掉落图纸 (2% 掉率)
    ]
  }
}
```
