# 练习场 (Practice Ground) v1.0

## 元数据

| 项 | 内容 |
|----|------|
| 编号 | 16 |
| 日期 | 2026-09-19 |
| 状态 | 已实现（后端接口与配置随本文档落地；前端页面规格由 ebs-duo 侧另行立项） |
| 服务对象 | ebs-duo 前端练习场页（选一场 → 真接口打完 → 出结算的全链路验证） |
| API 入口 | `GET /battle/practice`（`src/api/presentation_api.py`） |
| 配置文件 | `data/practice_scenarios.json` |
| 上游文档 | [14.battle_timeline_contract.md](./14.battle_timeline_contract.md)（simulate 战报契约——练习场的每一场都由它定义输出） |
| 文档定位 | 练习场对局列表接口与配置的**单一事实来源（SSOT）**；跨端立绘解析规则（后端只报 ID）的裁决落点。本文档不管战斗怎么打（Doc 1/14/15 的事），只管"有哪些场可打、怎么列出来、立绘怎么对上号"。 |

---

## 1. 定位与范围

练习场是**前后端全链路验证的最小产品面**：一张只读的对局列表，前端选一场，拿条目里的机体 ID 调 `POST /battle/simulate`，播放战报、出结算。它的存在意义是让"配置 → 列表 → 开战 → 战报 → 结算"这条链随时可以真枪实弹地走一遍——不登录、不进副本、不动数据库。

**练习场不是**：

- **不是 PVE**：无会话、无锁定队伍、无掉落与经济、无属主。PVE 的探索循环见 Doc 10/13，与本模块无共享状态。
- **不是机体图鉴**：列表只给对局的名字/描述/敌我机体 ID；机体档案（名称、面板、立绘键）随战斗数据 `init` 块下发（Doc 14 §4）。
- **不发明第二个开战入口**：列表条目即 `BattleRequest` 的材料，simulate 是唯一执行通道——避免"列表能选、打不起来"的契约漂移。

## 2. 接口契约

### `GET /battle/practice`

- **鉴权**：无（纯静态配置投影，与 `/health` 同级；simulate 本身也支持匿名）。
- **语义**：只读、无状态、不触战斗引擎；响应顺序即配置文件顺序（运营策展顺序）。

响应（`List[PracticeScenarioItem]`）：

```json
[
  {
    "name": "初次出击",
    "description": "联邦的白色恶魔对阵红色彗星——一年战争的经典对局……",
    "mecha_a_id": "mech_rx78",
    "mecha_b_id": "mech_zaku"
  }
]
```

| 字段 | 类型 | 说明 |
|---|---|---|
| `name` | string | 对局名（列表卡片标题） |
| `description` | string | 一句话描述（卡片副文案，运营可写战术提示或风味文本） |
| `mecha_a_id` | string | 我方机体配置 ID（画面左侧；侧位语义对齐 Doc 14 §1.4：`a`=己方） |
| `mecha_b_id` | string | 敌方机体配置 ID（画面右侧） |

**契约最小化红线**：条目字段恰为以上四项。不泄露机体面板/胜率等任何数值（选场不该变成攻略），不携带任何图片资源引用（见 §4）。配置文件中的 `id` 字段是加载容器键，**不进响应**——对局在本链路中没有独立身份，`mecha_a_id × mecha_b_id` 即其全部语义。

**消费方式**：前端取条目直接 `POST /battle/simulate` `{"mecha_a_id": …, "mecha_b_id": …}`，返回 Doc 14 契约的 `TimelineDocument`，结算读 `result` 块。每次请求都是全新随机对局（Doc 14 §2 语义），练习场不做种子固定。

## 3. 配置文件与加载规则

`data/practice_scenarios.json`（数组，逐条 `PracticeScenarioConfig`）：

| 字段 | 必填 | 说明 |
|---|---|---|
| `id` | ✅ | 加载容器键（去重用），不进对外契约 |
| `name` / `description` | ✅ / 默认空 | 见 §2 字段表 |
| `mecha_a_id` / `mecha_b_id` | ✅ | 必须是 `data/mechas.json` 中真实存在的机体 ID |

加载规则（`src/loader.py`）：

- **内容级容缺**：文件不存在 = 无练习场（返回空列表），不阻断启动——与 `weapons.json` 合并加载同款先例；这是内容文件不是系统文件。
- **交叉校验前置**：引用了不存在机体的条目在**加载期剔除**并告警，不进运行时——"列表能选、打不起来"（simulate 404）的契约漂移在启动时暴露。

## 4. 立绘规则（红线，跨端）

> 本节是 Doc 14 §9.1 待裁决的**裁决落点**（2026-09-19 用户拍板，方案 A）。

1. **后端契约只报配置 ID**：`mecha_a_id`/`mecha_b_id`（及战报 `init` 块中的 `mecha_id`、未来的 `pilot` 键）。任何对外接口**不得**下发图片路径、URL、文件名或资源版本号。
2. **前端建"配置 ID → 图片路径"对照表**：立绘资产挂在前端仓库，键为配置 ID；新增机体 = 后端加配置 + 前端补一行对照，两仓各自演进。
3. **缺图降级归前端**：对照表查不到 = 该机体无立绘，按前端自有降级处理（先例：ebs-duo 2.3 §4.4"缺什么退什么，战斗照常"）。后端不感知图片存在性。
4. **后端内部字段不进契约**：`data/mechas.json` 的 `portrait_id`/`model_asset` 是后端内部字段，不属于对外契约；战报 `init.portrait` 当前为占位常量，前端不得依赖其内容（清理时点见 Doc 14 §9.1 裁决附注）。

**为什么定死这条**：不定它，练习场接真接口后所有机体都没立绘——不是"缺图的难看"，是"全军没图"。后端报 ID 是唯一两边都成立的稳定契约：资源路径、命名规范、打包方式全是前端内部实现自由。

## 5. 边界与非目标

- ❌ 不做对局结果持久化/练习战绩统计（战斗数据本就不落库，Doc 15 §3）。
- ❌ 不做自选机体自由搭配（那是调试设施 URL 参数的事，ebs-duo 2.1 §A4 已有；练习场只列策展局）。
- ❌ 不做难度/奖励/解锁（练习场无经济语义）。
- ✅ 但保留演进空间：未来"训练模式"（Doc 15 §3 场景标签 `training`）若要会话化/种子化，在此文档上立新版修订，不另起炉灶。

## 6. 变更记录

| 版本 | 日期 | 变更 |
|---|---|---|
| 1.0 | 2026-09-19 | 首版：`GET /battle/practice` 只读对局列表契约、`practice_scenarios.json` 配置 schema 与加载校验规则、跨端立绘规则红线（Doc 14 §9.1 裁决落点）；随接口一并实现（含契约测试） |
