# ebsp 项目地图

## 元数据
<!-- auto:meta -->
- 栈：Python@.
- 新鲜度：验证于 @220d647（refresh.py 自动刷新；语义条目见下方 drift 报告）
- ⚠️ 本文件由 naofu skill 的提交钩子随代码自动刷新；若你的环境没有这套 skill，内容可能落后于代码——请以代码为准
<!-- /auto:meta -->

| 项 | 内容 |
|----|------|
| 项目 | ebsp |
| 关联 | CLAUDE.md、PRD / 0号文档 |

---

## 1. 约束文件（policy / 规则）
- **CLAUDE.md** — 技术规范 / 命令 / 结构 ｜ 相关: 写代码前 / 查约定时 ｜ @
- **AGENTS.md** —（若有）跨工具 agent 约定 ｜ @
- **PRD / 0号文档** — 需求与设计总思路 ｜ 相关: 定方向 / 裁决范围时 ｜ @

## 2. 基础工具
<!-- auto:m2 -->
### 仓库根 （Python）
- 规模：18,935 行（中型 · Python · Web 服务）
- 已有 7 类工具：Git 版本管理（Git）, 静态类型检查（pyright）, 语言服务器 (LSP)（pyright / pylsp）, 自动化测试（pytest）, 环境与命令（uv）, 代码文档注释（ruff）, 跳过测试管理（pytest）
- 建议装 12 类：代码格式化, 代码规范检查, 模块测试覆盖, 规范规则配置, 测试覆盖率, 提交前检查
<!-- /auto:m2 -->

## 3. 指引文件（结构骨架 / 导航）
<!-- auto:m3 -->
> 📜 API 签名概览见 [`项目骨架.md`](docs/项目骨架.md)（refresh 自动生成，按被依赖权重排序；地图只放索引，签名单放以免每次全读过重）。

> ⚠️ 体量热点 12 处：src/combat/engine.py, src/models.py, src/user/item_system.py …——明细与阈值见骨架「体量热点」区；落点命中 → 先答「不再增宽」还是「带拆分」。

### 仓库根 （Python）
- 契约：models.py · 81/113 集中（散落 9 处：src/combat/statistics_collector.py(3), src/combat/entry.py(1), src/combat/engagement.py(3), src/api/presentation_api.py(2), src/pve/battle_bridge.py(1)）
- 模块（62）：src/api/context.py, src/api/errors.py, src/api/inventory_api.py, src/api/presentation_api.py, src/api/pve_api.py, src/api/user_api.py, src/combat/calculator.py, src/combat/engagement.py, src/combat/engine.py, src/combat/entry.py, src/combat/resolver.py, src/combat/statistics_collector.py …
- ⚠ lib 耦合框架 3 处：src/user/auth.py→fastapi, src/user/dependencies.py→fastapi, src/api/errors.py→fastapi
<!-- /auto:m3 -->

**语义条目**（refresh 自动写草稿 + 人审核定稿；格式 `- **<path>** — <用途> ｜ 职责: <管什么/不管什么（编排宿主必填）> ｜ 相关: <触发词> ｜ 依据: <doc> ｜ @<commit>`；可选 `〔范式样板·<类别>〕` 标记项目最佳范例，新建同类模块先实读对标）：

> 📝 下方 auto:drafts 区是 refresh 自动写的语义草稿（用途/相关由模块 docstring + 路径 + 公开签名
> 推断，人只审不写）。审核通过 → 改写成下方正式条目格式（`- **path**`）并删掉对应草稿行；
> 正式条目会自动脱「新增」、不再被重草。

<!-- auto:drafts -->

<!-- /auto:drafts -->

正式语义条目（人维护）：
- **src/user/item_system.py** — 物品系统唯一门面（Doc 17）：玩家一切家当的进出只走此门——发放（凭票据）、消耗、丢弃（背包内+寄存物逐件）、锁定/解锁四招与货币两腿（charge/grant），票据/临时货舱状态机、外部发放台账、五道检查（容量/归属/锁定/余额/防重复回执）全在此集中 ｜ 职责: 管 ItemSystem 全部资产变动方法与 GrantManifestMismatch/InsufficientCredits/CapacityShortfall/TicketState/ItemLocked 五类错误载体、make_receipt_id 回执单点、TicketStatus 四态枚举；不管 穿戴/卸下（equipment_service，容量经 InventoryService——穿戴招的既定分工）、不管容量占用算法细节（复用 InventoryService）、不管 HTTP 翻译（api 层）；交换=调用方同事务 charge_credits+玩法入库（母舰购买为样板），玩法不得绕门直改 user.credits/资产表 ｜ 相关: 发放/领取/丢弃/锁定物品时, 动信用点账时, 查临时货舱时, 碰票据状态机或回执时, 新玩法要动玩家资产时 ｜ 依据: 模块 docstring「物品系统唯一门面（Doc 17）」；末批全量审资产写入点核查：credits 仅门面两腿、装备材料仅 InventoryService.add_assets ｜ @220d647
- **src/api/errors.py** — 发放清单不符（mismatch）的 HTTP 收场单点：先 commit 让 rejected 留档票据落库、再统一翻译 400「本批未发放，已记录」 ｜ 职责: 管 commit_and_report_mismatch 与 detail 常量；不管 各 router 其余错误翻译（inventory_api._translate_facade_error） ｜ 相关: 接新的发放来源时, 改 mismatch 报错文案时 ｜ 依据: 模块 docstring 及三发放方（pve_api/user_api/inventory_api）调用点 ｜ @220d647
- **src/api/context.py** — API 层全局共享对象（DataLoader）的存取点，用以避免模块间循环引用 ｜ 相关: 改全局 loader 获取时, 新路由要用 DataLoader 时, 遇循环引用要解耦时, loader 未初始化报错时 ｜ 依据: docstring「API 内容上下文…存放全局共享的对象（如 DataLoader），避免循环引用」；全文仅 _loader 单例 + get_loader/set_loader 两个函数 ｜ @8d460a1
- **src/api/inventory_api.py** — 物品系统 HTTP 接口（Doc 17）：票据/临时货舱三端点、锁定解锁、装备与材料丢弃、容量状态、家当分类列表，及调试用随机装备生成（已走统一门面） ｜ 职责: 管 /inventory 十端点（status/items/tickets 列表+accept+discard/equipments lock·unlock·discard/items discard/debug generate-item）的编排与错误翻译单点（_translate_facade_error：Capacity→400+shortfall/TicketState→409 刷新/ItemLocked→400 先解锁/mismatch→commit 留档后 400/其余 ValueError→404）；不管容量计算、五道检查、票据状态机与台账（归 ItemSystem 门面）、不管装备词条生成（EquipmentGenerator）；旧 POST /finalize 超载通道已退役（Doc 17 接管 Doc 12，防旁路） ｜ 相关: 改背包接口时, 碰物品发放/票据时, 锁定/丢弃确认时, 查货舱容量与待处理提醒时, 调试生成装备时 ｜ 依据: docstring「物品系统 REST API…参考文档：Doc 17」；十端点实读核对 ｜ @74a561a
- **src/api/presentation_api.py** — FastAPI 主应用：装配全部 /api 子路由与生命周期初始化，并自带 /battle/simulate 战斗模拟与练习场列表端点 ｜ 职责: 管应用装配（user/inventory/pve 路由挂载到 /api）、startup/shutdown 生命周期（init_db、DataLoader、演出模板、HeartbeatGuard 启停）、/health、/battle/practice、/battle/simulate 的出战资格校验与错误码翻译；不管各子域业务逻辑（归 user/inventory/pve 各 router 与服务层）、不管战斗裁定（Engagement/BattleEntryService） ｜ 相关: 改主应用启动流程时, 加新路由挂载时, 改 /battle/simulate 时, 练习场列表时, 改服务启停钩子时 ｜ 依据: docstring「FastAPI 主应用文件」；app.include_router 三路由挂 /api；startup 依次 init_db/DataLoader/演出模板/HeartbeatGuard；注释引 Doc 7 §11.1、Doc 14、Doc 15 §5、Doc 16 ｜ @8d460a1
- **src/api/pve_api.py** — PVE 副本流程的 HTTP 接口：查子区域、进副本、推进事件、接战、重放战报、撤退/强退、会话心跳 ｜ 职责: 管 /pve 八端点（zones/enter-region/advance/engage/battle 重放/extract/abandon/heartbeat）、会话属主 404 校验（get_pve_session_or_404）、母舰解析 404 翻译与事件可见性裁剪；不管事件推进不变量（PveSessionData）、战斗裁定（BattleBridge）、奖励结算（RewardController）、母舰解析机制（PveEntryService） ｜ 相关: 改 PVE 接口时, 进副本/推进事件时, 触发遭遇战时, 撤退结算/强退时, 会话 404 属主校验时, 心跳超时回收时 ｜ 依据: 实读确认候选推断：router=APIRouter(prefix=/pve)，恰 8 端点；注释引 Doc 7 v2.2 §11.3、Doc 14 §9.5、Doc 15 §5/§6 ｜ @8d460a1
- **src/api/user_api.py** — 用户系统的 HTTP 接口：注册登录、新号引导状态、初始机体领取，及机体/编队/母舰资产管理 ｜ 职责: 管 /user 下注册/登录与 JWT 签发、/me 引导状态位、claim-starter 领取、编队创建/出战切换、母舰目录/购买/切换端点的编排与错误码翻译；不管认证依赖实现（src/user/dependencies）、不管仓储与服务层逻辑（UserRepository/OnboardingService/MothershipService/UserAssetRepository） ｜ 相关: 改注册登录时, 签发/调试 JWT 时, 领初始机体时, 改编队/母舰接口时, 新号引导状态时 ｜ 依据: docstring「用户 API 路由…提供用户注册、登录、资产管理的 HTTP 接口」；端点 register/login/me/claim-starter/mechas/squads/motherships；注释引 Doc 7 v2.2 §11.4/§11.5 ｜ @8d460a1
- **src/combat/calculator.py** — 战斗数值公式库——熟练度、气力、护甲减伤、精准削减等纯计算静态函数集合 ｜ 相关: 改战斗数值公式时, 调熟练度/气力/护甲减伤公式时, 碰未命中率计算时 ｜ 依据: class CombatCalculator docstring「战斗计算核心」；7 个无状态 @staticmethod 公式；resolver.py 复用其中 5 个，calculate_will_stability_bonus 全仓零引用、calculate_precision_reduction 仅 scripts 使用（裁决改：起草误计 6 个） ｜ @8d460a1
- **src/combat/engagement.py** — 战斗裁判——把打一场仗封装成黑盒：两份冻结快照进，引擎与演出只跑一次，战报与裁定同源出 ｜ 职责: 管单场战斗的裁定执行与战报（TimelineDocument+ResultBlock）装配；不管快照从哪来（entry.py）也不管裁定后的写回（battle_bridge） ｜ 相关: 改战斗裁定流程时, 改战报结构装配时, 碰裁定封闭/值冻结不变量时, 改本场随机流注入时 ｜ 依据: 模块 docstring「战斗裁判 (Engagement) — Doc 15 §3」；docs/15.battle_engagement.md 存在；resolve() 二次调用抛 RuntimeError 佐证裁定封闭不变量 ｜ @8d460a1
- **src/combat/engine.py** — 战斗引擎——先手判定、武器选择、回合主循环与演出时间轴生产，含全仓库唯一终局判定 derive_verdict ｜ 职责: 管回合主循环/先手/武器 AI/EN 回能/演出事件生产与监听器注册；不管快照装配（entry.py）与战报契约装配（engagement.py） ｜ 相关: 改战斗主循环时, 改先手判定规则时, 改武器 AI 选择时, 碰终局判定(ko/decision/draw)时, 改 HOOK_* 钩子调用点时 ｜ 依据: 模块 docstring「战斗引擎 包含先手判定、武器选择和战斗主循环」；derive_verdict docstring「全仓库唯一的终局判定实现」且被 engagement._derive_ruling 引用（对抗审核全仓交叉验证成立） ｜ @8d460a1
- **src/combat/entry.py** — 战斗入口收发室——调试/PVE 各界面进战斗的快照装配唯一出口 ｜ 职责: 管快照装配（配置直构/存档还原/环境效果注入/敌方模板缩放/时间回能残血注入）并产出 EngagementSpec；不做裁定（Engagement）、不做 PVE 写回（battle_bridge） ｜ 相关: 改战斗快照装配时, 改调试/PVE 进战斗来源时, 碰规则环境注入时, 改敌方缩放系数时 ｜ 依据: 模块 docstring「战斗入口（收发室）— Doc 15 §4 红线 5：装配单点」；EngagementSpec 全 src 仅 entry.py 两处构造；build_pve 走 MothershipIntegrationService.calculate_regeneration 时间回能 ｜ @8d460a1
- **src/combat/resolver.py** — 攻击结算圆桌判定——按 MISS>躲闪>招架>格挡>暴击>命中优先级分段裁定单次攻击结果并计算伤害 ｜ 相关: 改圆桌判定概率时, 攻击命中/暴击/招架结算时, 碰伤害计算流程时, 改精准削减公式时 ｜ 依据: 模块 docstring「Combat resolution system using round table mechanics.」；_build_segments_from_data 按 MISS>DODGE>PARRY>BLOCK>CRIT>HIT 优先序分段；唯一调用方 engine._execute_attack ｜ @8d460a1
- **src/combat/statistics_collector.py** — 战斗统计收集器——事件驱动订阅 RawAttackEvent 流，产出单场战斗的判定分布/伤害/技能触发等多维统计 ｜ 相关: 改战斗统计指标时, 碰批量模拟报表时, 改攻击事件消费逻辑时 ｜ 依据: 模块 docstring「从 RawAttackEvent 事件流中收集战斗统计数据」；类 docstring「只负责收集统计，不干预战斗逻辑」；注意：src 生产链路无注册方，全仓唯一实际注册在 scripts/sim/sim_challenge_boss.py ｜ @8d460a1
- **src/config.py** — 游戏战斗全局数值配置常量类（气力、回合、距离、判定概率、伤害与熟练度公式参数等平衡性数值的主要收口） ｜ 相关: 调平衡数值时, 改战斗公式参数时, 调气力/距离/暴击率等常量时, 找硬编码数值默认值时 ｜ 依据: docstring「游戏全局配置常量…存放所有硬编码的数值参数，便于后续调整平衡性」；class Config 仅静态常量（裁决改：唯一收口→主要收口，UPGRADE_SCALING 等部分缩放常量仍在 core/factory 与 item_generator） ｜ @8d460a1
- **src/core/factory.py** — 快照服务层 SnapshotFactory：编排 DB 用户养成数据与静态配置，换算 upgrade_bonuses 后委托 MechaFactory 生成用户战斗快照 ｜ 职责: 管 DB 养成数据获取（UserAssetRepository）、升级加成换算、已装配装备过滤排序与快照编排；不管属性计算（统一委托 src/factory.py 的 MechaFactory） ｜ 相关: 从用户存档生成战斗快照时, 接用户驾驶员/装备进快照时, 改快照服务编排流程时, 区分与 src/factory.py 职责时 ｜ 依据: docstring「职责：从 DB 获取用户养成数据…委托 MechaFactory 完成最终的快照组装。自身不包含任何属性计算逻辑」；与 src/factory.py 纯计算形成分层 ｜ @8d460a1
- **src/core/item_generator.py** — 装备随机生成器 EquipmentGenerator：按 Doc 8 规则掷 ilvl 浮动、T0-T4 词条档位与技能槽，产出可直接入库的随机属性字典 ｜ 相关: 改装备随机词条生成时, 调词条档位/技能概率时, 生成新装备实例数据时, 掉落产出装备时 ｜ 依据: class docstring「物品生成器 - 负责创造具有随机潜力的装备 (Doc 8)」；generate_equipment 返回 {ilvl, affixes, skill}（对应 DB random_stats），含 TIER_WEIGHTS/SKILL_CHANCE 常量；候选「仅导出」不准，此为定义处 ｜ @8d460a1
- **src/database/base.py** — SQLAlchemy 异步数据库基础设施：连接配置（env 切换 SQLite/PostgreSQL）、异步引擎与会话工厂、ORM 基类 Base、时间戳/软删除 Mixin 及建表/关库函数 ｜ 相关: 改数据库连接配置时, 动 ORM 基类或通用 Mixin 时, 初始化/关闭数据库时, 切 PostgreSQL 时 ｜ 依据: docstring「数据库基础设施配置…确保代码可无缝迁移到 PostgreSQL」；实含 DATABASE_URL/async_engine/AsyncSessionLocal/Base/TimestampMixin/SoftDeleteMixin/init_db/close_db ｜ @8d460a1
- **src/database/models.py** — 用户与游戏存档的 SQLAlchemy ORM 表定义：用户账户、机体/驾驶员/装备/物品/编队/母舰资产、战斗录像、PVE 进度与会话、收益流水（11 张表） ｜ 相关: 改数据表结构时, 加用户资产/存档表时, 动表关系或 JSON 字段时, 找某张表的 ORM 定义时 ｜ 依据: docstring「数据库 ORM 模型定义…使用 sqlalchemy.JSON…支持软删除和多存档位」；恰 11 个 __tablename__（users 至 pve_reward_ledger） ｜ @8d460a1
- **src/database/session.py** — 数据库会话管理：FastAPI 依赖注入 get_async_session（每请求独立会话、自动提交/回滚/关闭）及测试与后台任务用的会话创建辅助 ｜ 相关: 接口注入 DB 会话时, 改会话生命周期/事务提交策略时, 测试或后台任务要建会话时 ｜ 依据: docstring「数据库会话管理 提供 FastAPI 的依赖注入函数…确保每个请求使用独立的异步会话」；get_async_session yield+commit/rollback/close，另有 create_session/ensure_session ｜ @8d460a1
- **src/factory.py** — 纯计算的快照工厂 MechaFactory：从静态配置组装 MechaSnapshot/WeaponSnapshot，聚合主副驾驶属性、技能、升级加成、装备修正与随机词条（不触 DB） ｜ 相关: 改快照组装逻辑时, 聚合驾驶员/副驾驶属性时, 加装备词条或升级加成计算时, 建新武器快照时, 区分与 src/core/factory.py 职责时 ｜ 依据: docstring「Factory module for creating runtime snapshots from configurations」；MechaFactory 全静态方法，仅 import models 与 presentation.constants，无 DB/Session ｜ @8d460a1
- **src/loader.py** — 静态配置中心 DataLoader：从 data/*.json 读取并解析全部游戏静态配置为 Pydantic 模型，缓存并提供按 ID 检索 ｜ 职责: 管 data/*.json 静态配置的加载、校验剔除与按 ID 检索；不管运行时快照组装与属性计算 ｜ 相关: 加新配置表时, 改 data/*.json 读取解析逻辑时, 配置加载报错/撞键处理时, 找配置 getter 时 ｜ 依据: docstring「数据加载器 (Loader)」；load_all 覆盖 pilots/equipments/mechas/motherships/regions/affixes/instances/environments/practice_scenarios 并做引用交叉校验剔除 ｜ @8d460a1
- **src/models.py** — 核心类型定义模块：核心战斗枚举、Pydantic 静态配置模型（机体/装备/驾驶员/母舰/区域/词条/副本/环境/练习场）、运行时快照模型（MechaSnapshot/WeaponSnapshot）及战斗回合上下文 BattleContext、技能系统 dataclass（Effect/TriggerEvent/BuffState 等） ｜ 相关: 加改模型字段时, 动快照结构时, 加配置模型或枚举时, 改 BattleContext 时, 找 Effect/TriggerEvent 定义时 ｜ 依据: docstring「数据模型定义 包含所有枚举类型、配置模型 (Pydantic) 和快照模型 (Snapshot)」；807 行含 BattleContext、Modifier/Effect/Condition、TriggerEvent/BuffState（裁决改：全部枚举→核心战斗枚举，演出枚举在 presentation/constants.py） ｜ @8d460a1
- **src/presentation/assembler.py** — 演出 L3 动态丰满层：把 L2 竞标选中的骨架拼装为最终攻防文本——三段式拼装 + DHL 动态受击部位 + SVI 语义变量注入 + 损伤量级分级 ｜ 相关: 改 L3 拼装文本时, 改受击部位映射 DHL 时, 改 SVI 变量注入时, 改伤害分级/状态词时, 模板缺失兜底文本行为 ｜ 依据: docstring「L3 动态丰满层 - 原子化组合 + DHL + SVI…三段式拼装」；含 DhlMapper/DamageGrader/SVI/TextAssembler 四类，bone 缺失回落 constants.T3_FALLBACK_TEXTS ｜ @8d460a1
- **src/presentation/av_dispatcher.py** — 演出 L4 视听调度层：把文本演出翻译成视听事件——按优先级规则树选镜头、语义化时间轴延迟、默认动画映射与伤害显示规则，产出 ACTION/REACTION 双事件 ｜ 职责: 管镜头选择/时间轴延迟计算/默认攻防动画与伤害显示映射；不管文本拼装与模板竞标（分属 L3 assembler 与 L2 bidder） ｜ 相关: 改镜头选择规则时, 改演出时间轴延迟时, 改默认动画映射时, 改伤害显示规则时, 动视听调度 dispatch 时 ｜ 依据: docstring「L4 视听调度层 - 影视级调色」；CAMERA_RULES 按 priority 降序匹配；dispatch(DispatchPayload) 返回 (action, reaction) 元组 ｜ @8d460a1
- **src/presentation/bidder.py** — 演出 L2 剧本解构层：攻守分离的双轨独立竞标器，为 Action（按 MotionStyle）与 Reaction（按频道+材质）分别选骨架模板并管理模板冷却；降级链 Reaction 轨 T2→T2.5→T3、Action 轨 T2 精确→T2 通用材质→T3 ｜ 相关: 改模板竞标时, 改 T2/T2.5/T3 降级匹配链时, 改模板冷却机制时, 改动反双轨独立匹配时, 加权随机选择逻辑 ｜ 依据: docstring「L2 剧本解构层 - 动反双轨独立竞标…10 种攻击 + 10 种受击 = 100 种组合」；_bid_reaction 注释引「文档 6 机制 3」（裁决改：T2.5 层仅 Reaction 轨有，Action 轨无） ｜ @8d460a1
- **src/presentation/constants.py** — 演出系统公共枚举与常量定义：演出频道 Channel、动作风格 MotionStyle、宏观动作 MacroMotion（含 MOTION_STYLE_TO_MACRO 映射）、伤害材质 DamageMaterial、模板层级 TemplateTier（T0_SCRIPTED/T0_LETHAL/T1/T2/T2.5/T3_FALLBACK，无 T3.5）、T3 兜底文本与 MDDC 默认值 ｜ 相关: 改演出枚举时, 加动作风格/伤害材质时, 改 T3 兜底文本时, 改模板层级定义时 ｜ 依据: 实读确认导出全集；被 13 个文件 import（含 src/factory.py 与 presentation 内 8 文件）；裁决修正：TemplateTier 实际成员无 T3.5 ｜ @8d460a1
- **src/presentation/contracts.py** — 战斗时间轴对外契约模型（Doc 14 §3-§7）：以 Pydantic 定义前端消费的时间轴 JSON 结构 TimelineDocument 及 meta/init/rounds/result 各子块，是战报格式的唯一 schema 实现点 ｜ 相关: 改时间轴契约字段时, 改前端战报 JSON 结构时, 改 Doc 14 同步三处流程时, 重录金样张前, 动 simulate response_model 时 ｜ 依据: docstring「战斗时间轴契约模型 — Doc 14 §3-§7…战报格式唯一拼装点（架构红线 1）」；对抗审核验证：TimelineDocument 仅 engagement.py:243 构造、presentation_api.py:166 作 response_model，无第二实现点 ｜ @8d460a1
- **src/presentation/event_builder.py** — 战斗引擎与演出系统间的适配层：静态工厂把引擎计算结果（机体快照/武器/上下文/判定）打包为 RawAttackEvent，使引擎不感知表现层数据结构 ｜ 相关: 改 RawAttackEvent 构建时, 改引擎到演出字段映射时, 武器新增 motion_style/damage_material 透传时 ｜ 依据: docstring「攻击事件构建器…AttackEventBuilder 是战斗引擎（engine.py）和演出系统之间的适配层…MDDC v5.1 (Doc 6)」；全库唯一 RawAttackEvent 构造点，唯一调用方 engine.py ｜ @8d460a1
- **src/presentation/loader.py** — 模板加载器：把 YAML 演出模板配置解析为 ActionBone/ReactionBone 对象，供 TemplateRegistry 使用，含 T2.5_Decay 层自动识别 ｜ 相关: 改 YAML 模板解析时, 改 bone 字段解析/默认值时, 新增模板配置字段时, 模板加载失败降级行为 ｜ 依据: docstring「模板加载器 - Template Loader…作为 TemplateRegistry 的底层加载器」；_parse_reaction_bone 未显式给 tier 且 GENERIC 材质时自动设 T2_5_DECAY；T0 脚本模板不走配置 ｜ @8d460a1
- **src/presentation/mapper.py** — 演出系统核心编排器（CPS v5.0）：唯一入口 map_attack() 将 RawAttackEvent 经 L1 ODR 路由→T0 脚本拦截→L2 双轨竞标→L3 原子拼装→L3.5 DispatchPayload→L4 视听调度，产出演出事件序列 ｜ 职责: 管 L1 路由→T0 拦截→L2 竞标→L3 拼装→L4 调度的全流程编排、rng 注入与回合冷却推进（advance_turn）；不管模板匹配规则/文本拼装细节/镜头时间轴规则（分属各层组件） ｜ 相关: 改演出流水线编排时, 动演出入口 map_attack 时, 改 L1-L4 串联顺序时, 改 T0 脚本拦截逻辑时, 排查演出事件生成问题 ｜ 依据: docstring「Event Mapper - 演出系统核心编排器 (CPS v5.0)…四层架构的唯一入口」；对抗审核逐环验证管道顺序与代码一致，唯一外部调用方 combat/engine.py:758 ｜ @8d460a1
- **src/presentation/models.py** — 演出系统数据模型（dataclass）：RawAttackEvent（引擎产出/演出输入）、PresentationAttackEvent（前端视听指令/演出输出）、DispatchPayload（L3→L4 层间 DTO）、PresentationAttackSequence 与 PresentationRoundEvent（序列/回合分组） ｜ 相关: 改 RawAttackEvent 字段时, 改演出事件模型字段时, 加层间 DTO 字段时, 动统计扩展字段(*_after/roll_value)时 ｜ 依据: 恰 5 个 @dataclass；类 docstring「input for the Presentation System」「output」「L3 Assembler → L4 AVDispatcher 的数据传递」；被 combat/engine、engagement、statistics_collector 等十余处 import ｜ @8d460a1
- **src/presentation/registry.py** — 模板注册中心：从配置加载并持有 ActionBone/ReactionBone 库作为 L2 双轨竞标的数据源，并提供进程内共享注册表单例（API 启动期唯一加载点） ｜ 职责: 管 ActionBone/ReactionBone 库的加载、存储与全局共享单例（initialize_shared_registry/get_shared_registry）；不管 YAML 解析细节（loader 承担）与竞标匹配（bidder 承担） ｜ 相关: 改模板注册加载时, 动共享注册表单例时, 改模板配置路径初始化时, 排查模板库为空问题时 ｜ 依据: docstring「模板注册中心 - Template Registry…数据流向 YAML→TemplateLoader→TemplateRegistry→DualBidder」；对抗审核验证：initialize_shared_registry 全库唯一调用点为 startup 钩子，「唯一加载点」成立 ｜ @8d460a1
- **src/presentation/renderer.py** — 渲染器层——将演出事件格式化为带 ANSI 颜色的终端文本，供调试与开发阶段可视化 ｜ 相关: 演出文本渲染, 终端演出输出, 战斗演出可视化, ANSI 颜色演出, TextRenderer ｜ 依据: docstring「L4 AV Dispatcher 的下游…不涉及业务逻辑，只负责序列化/格式化」；:17-18 历史注记指已删除的 JSONRenderer（非现存代码）；TextRenderer 为活代码，被 combat/engine.py:370 实例化、verbose 下 :777 调用 ｜ @8d460a1
- **src/presentation/router.py** — L1 绝对律令层——按「结局>意图>技能」优先级把原始攻击事件路由为演出频道（FATAL/EVADE/IMPACT/SPECIAL） ｜ 相关: L1 路由, 结局前置路由, 演出频道路由, OutcomeRouter, 致死判定 ｜ 依据: docstring「结局（Result）> 意图（Intent）> 技能（Skill）」；_ROUTING_RULES：is_lethal→FATAL / counter|support→SPECIAL / MISS|DODGE|PARRY→EVADE / 默认 IMPACT；被 mapper.py:106 调用 ｜ @8d460a1
- **src/presentation/scripted_manager.py** — T0 剧本强制层——供外部剧本按(回合,攻方,守方)注入高优先级演出模板，完全绕过 L1-L4 常规竞标流程 ｜ 职责: 管强制演出模板的注入/查询/清空（键为 round+attacker_id+defender_id）；不管模板执行/渲染与正常竞标流程 ｜ 相关: T0 剧本强制, 强制演出模板, 剧情杀, 脚本注入演出, ScriptedPresentationManager ｜ 依据: docstring「优先级 T0_SCRIPTED > T0_LETHAL > T1…」「演出系统的后门」；inject/get/clear 三方法；被 mapper.py:60 实例化持有 ｜ @8d460a1
- **src/presentation/template.py** — 演出模板数据类型定义——旧格式大一统模板（PresentationTemplate 及 Conditions/Content/Visuals，保留用于 T0 脚本模板与向后兼容）与新格式原子骨架（ActionBone/ReactionBone） ｜ 相关: 演出模板定义, ActionBone, ReactionBone, PresentationTemplate, 模板数据结构, 原子骨架 ｜ 依据: PresentationTemplate docstring「(旧格式 - 大一统模板). 保留用于 T0 脚本模板和向后兼容」；registry.py:24 同注；ActionBone/ReactionBone 为新格式，供 bidder/assembler/loader/registry 使用（候选需补全，裁决采纳） ｜ @8d460a1
- **src/pve/battle_bridge.py** — PVE 与战斗层的桥接：委托收发室装配与裁判裁定，把裁定结果一次性写回会话并产出结算摘要与掉落 ｜ 职责: 管装配→裁定→写回会话的衔接、战利品生成与战报暂存；不管战斗规则本身（归 BattleEntryService/Engagement）与撤离折算入库（归 RewardController） ｜ 相关: 改 PVE 遭遇战结算时, 碰战斗写回残血/掉落时, 改战报暂存机制时 ｜ 依据: 类 docstring「PVE 与战斗编排层之间的桥接（Doc 15 §5 消费方表）…战斗知识零泄漏」；engage() 链路 build_pve→Engagement.resolve→一次性写回 HP/EN+战报暂存 session.battle_reports ｜ @8d460a1
- **src/pve/enums.py** — PVE 领域枚举定义：事件类型、会话生命周期状态、区域节点状态、战斗结果与退出方式 ｜ 相关: 改 PVE 事件/会话/区域状态枚举时, 加新事件类型或退出方式时 ｜ 依据: 实读共 5 个 str Enum（EventType/SessionStatus/ZoneStatus/CombatOutcome/ExitMethod）；被 9 个文件导入（含 src/api/pve_api.py） ｜ @8d460a1
- **src/pve/event_generator.py** — 按子区域配置随机生成一条线性 PVE 事件序列（固定遭遇+加权随机+末位强制 Boss） ｜ 相关: 改事件序列生成规则时, 碰固定遭遇/暗雷上限/Boss 收尾逻辑时, 兼容新旧区域配置格式时 ｜ 依据: 类 docstring「PVE 事件序列生成器…Doc 13 规范：固定遭遇位置、暗雷数量限制、末尾固定 Boss」；InstanceZoneConfig/ZoneConfig 双格式为代码侧兼容措施（代码自注+DEVLOG 记录），Doc 13 本文无「迁移期」表述（裁决修正引用） ｜ @8d460a1
- **src/pve/heartbeat.py** — 后台定时守卫：扫描内存 PVE 会话，超时无心跳即判定断线并销毁 ｜ 职责: 管超时会话的定时扫描与销毁；不管会话创建/业务结算/心跳刷新（get_session 取会话时顺带刷新） ｜ 相关: 改 PVE 断线即失败机制时, 碰会话超时清理时 ｜ 依据: 类 docstring「定时扫描内存中的 PveSession 以判定是否超时断线 (Disconnect = Fail)」；TIMEOUT_SECONDS=120/SCAN_INTERVAL=30；对应 docs/10 §7.3 ｜ @8d460a1
- **src/pve/models.py** — PVE 核心领域模型：事件/序列、队伍与敌方状态、临时奖励，以及可全量序列化的会话根 PveSessionData ｜ 相关: 改 PveSessionData 结构时, 碰事件推进/战报暂存清理时, 改跨战斗状态快照字段时 ｜ 依据: PveSessionData docstring「可被全量 JSON 序列化并存储在数据库中用于断线续连」（docstring 口径；Doc 10 §7.3 现行架构为断线即失败、无续连路径）；advance_event 统一清理过期战报暂存（Doc 15 §6）；src/ 内被 7 处导入 ｜ @8d460a1
- **src/pve/progress_service.py** — PVE 进度服务：大区域准入进度初始化、Zone 解锁状态查询（含隐藏节点按日概率刷新）与通关后解锁标记 ｜ 职责: 管 Zone 解锁状态、隐藏节点当日刷新、通关标记与后续解锁（持久化 UserPveProgress）；不管会话内事件推进与奖励结算 ｜ 相关: 改 PVE 区域解锁进度时, 碰隐藏节点刷新机制时, 通关后解锁下一节点时 ｜ 依据: 类 docstring「战略与战术层：PVE 大区域准入与小区域 (Zone) 进度管理服务」；对应 docs/10 §2/§3（含 §3.2 概率型隐藏节点） ｜ @8d460a1
- **src/pve/repository.py** — PveSessionData 与数据库 JSONB 之间的互换与存取仓储（落库时排除战报暂存） ｜ 相关: 改 PVE 会话持久化时, 碰断线续读取会话时 ｜ 依据: 类 docstring「负责将 PveSessionData 在 Pydantic 和 SQLAlchemy ORM JSONB 之间做互换和存储」；save_or_update 排除 battle_reports 落库（Doc 15 §6 裁决 #3）；Phase 2 简化：只 add 不 commit、get_by_user 仅返回未执行 stmt ｜ @8d460a1
- **src/pve/reward_controller.py** — PVE 收益控制器：会话期间暂存临时战利品，撤离结算时按退出方式折算（战败全失/紧急撤离征税）并正式入库 ｜ 职责: 管临时战利品暂存、结算折算与库存入库、幂等流水 PveRewardLedger、通关进度标记；不管战斗掉落的即时生成（BattleBridge）与信用点即时入账 ｜ 相关: 改 PVE 撤离结算时, 碰紧急撤离折损/战败惩罚时, 改奖励幂等防刷时 ｜ 依据: 类 docstring「PVE 收益控制器…根据退出方式决定最终的收益折算并入库」；DEFEATED 全失/EMERGENCY_EXIT 按母舰税率/通关全量；对应 docs/10 §6 ｜ @8d460a1
- **src/pve/schemas.py** — PVE API 的请求/响应 Pydantic DTO：脱敏事件视图、进入/推进/战斗/撤离等行为请求与返回 ｜ 相关: 改 PVE 接口出入参时, 碰前端脱敏视图字段时 ｜ 依据: 实读恰 12 个模型；visible_events 仅露 index<=current、pending_rewards 脱敏为数量；BattleResultResponse 注释「完整战报不经本响应下发（Doc 15 §5）」 ｜ @8d460a1
- **src/pve/services.py** — PVE 进入编排与母舰集成：完成进副本的校验、编队快照锁定与会话创建，并提供母舰对 PVE/背包/商店的钩子计算 ｜ 职责: 管 enter_region 全流程编排（母舰解析、准入与 Zone 进度校验、编队快照锁定）与母舰集成钩子（准入/战间恢复/撤离税率/货舱容量/商店上限/会话活跃检查）；不管会话内部结构（PveSessionManager）与战斗裁定 ｜ 相关: 改进入 PVE 副本流程时, 碰母舰战间恢复/紧急撤离税率时, 编队锁定快照时 ｜ 依据: MothershipIntegrationService docstring「提供母舰系统与外部环境 (如 PVE, 背包, 商店) 集成的逻辑层/钩子」；PveEntryService docstring「PVE 进入编排服务」；对应 docs/10 §2 准入与 §5 战间补给 ｜ @8d460a1
- **src/pve/session_manager.py** — PVE 会话的内存生命周期管理器：创建（事件序列生成+队伍初始化）、按 ID/用户查询、销毁与超时判定 ｜ 职责: 管内存会话注册表（类级字典+user 二级索引）、创建/查询/销毁/过期判定、取会话时刷新心跳；不管进度解锁、战斗结算与奖励折算 ｜ 相关: 改 PVE 会话创建/销毁时, 碰同用户并发会话限制时, 会话内存实现相关改动时 ｜ 依据: 类 docstring「PVE 会话生命周期管理器…目前处于 Phase 2 内存实现阶段」；_sessions+「user_id -> session_id 二级索引」；对应 docs/10 §7 ｜ @8d460a1
- **src/skill_system/_utils.py** — 技能系统的共享工具函数库：按 "self"/"enemy" 目标类型从战斗上下文解析效果作用的目标机体 ｜ 相关: 解析效果目标机体时, 条件/副作用要取 self 或 enemy 时, 改目标解析规则时 ｜ 依据: docstring「共享工具函数」；全文件仅 get_target() 一个函数；被 conditions.py 与 side_effects.py 复用 ｜ @8d460a1
- **src/skill_system/conditions.py** — 效果触发条件的检查器：按条件类型分派到 10 类检查函数（HP/气力/回合数/攻击结果/武器类型/ref_hook 等），全部满足才放行效果 ｜ 相关: 改技能触发条件时, 新增条件类型时, 排查技能不触发时 ｜ 依据: docstring「Condition Checker System 处理效果触发条件的检查逻辑」；_CONDITION_CHECKERS 恰 10 类（damage_type 为占位恒 True）；Doc 2 §1.1 模块表记载职责一致 ｜ @8d460a1
- **src/skill_system/effect_factory.py** — 数据驱动的效果工厂：从 data/skills.json 加载配置，按效果 ID 实例化 Effect 对象列表（含特性专用入口 create_trait_effects） ｜ 相关: 改 data/skills.json 效果定义时, 新增技能/精神指令/特性效果时, 效果未从配置生成时 ｜ 依据: 类 docstring「数据驱动的效果工厂」；_load_data 按多候选路径读 data/skills.json 并缓存；候选「仅导出」不准，此为 EffectFactory 定义处 ｜ @8d460a1
- **src/skill_system/event_manager.py** — 技能事件管理器：发布/订阅技能触发事件，维护回调、按技能统计触发率，以及回合级与单次攻击级的事件缓存 ｜ 职责: 管 事件回调注册/发布、触发统计、回合与单次攻击事件缓存；不管 技能效果运算与条件判定 ｜ 相关: 订阅战斗技能事件时, 改触发统计时, 取当回合/单次攻击事件时, 并行战斗事件隔离问题 ｜ 依据: docstring「技能事件管理器 - 轻量级事件发布/订阅系统」；实例优先+DualMethod 描述符委托模块级 _default_em 兼容旧类级调用；combat/engine.py 与 models.py 均使用 ｜ @8d460a1
- **src/skill_system/processor.py** — 技能效果核心处理引擎：在指定钩点上收集双方机体的效果，经条件过滤、优先级排序后依次应用数值/布尔运算或回调，并处理概率触发、副作用与次数耗尽 ｜ 职责: 管 单钩点效果收集/排序/条件判定/运算应用/概率触发/副作用触发/次数消耗/结果缓存(cached_results)；不管 效果定义来源(JSON)与效果生命周期(add/tick 归 skills.EffectManager) ｜ 相关: 改效果结算顺序/优先级时, 概率触发不生效时, 新增 operation 时, buff 次数耗尽逻辑, 递归保护问题 ｜ 依据: docstring「Effect Processor System 技能效果的核心处理引擎」；按 (priority, sub_priority, id) 排序应用；函数体内延迟 from ..skills import SkillRegistry 反查回调（受控互引避环）；Doc 2 §1.1 职责一致 ｜ @8d460a1
- **src/skill_system/side_effects.py** — 效果触发后的副作用执行器：按副作用类型分派执行消耗 EN、改变气力、施加新效果（Buff/Debuff）三类附加动作 ｜ 相关: 改效果触发后的附加动作时, 新增副作用类型时, 排查 EN/气力/Buff 被异常修改时 ｜ 依据: docstring「Side Effect Executor System 处理效果触发后的副作用执行」；_EXECUTORS 恰三类 consume_en/modify_will/apply_effect（consume_charges/modify_stat 注释标注未实现） ｜ @8d460a1
- **src/skills.py** — 技能与效果系统的入口/门面：SkillRegistry 注册钩子与回调并提供 process_hook 流水线（先跑传统钩子再委托 EffectProcessor），另含 cb_* 特性回调库、EffectManager（效果添加/tick）、TraitManager（特性初始化）、SpiritCommands（精神指令施放） ｜ 职责: 管 钩子/回调注册、process_hook 流水线入口、效果添加与 tick、特性转持久效果、精神指令施放；不管 底层效果运算/条件检查/副作用执行细节（委托 skill_system 包实现） ｜ 相关: 注册新技能回调时, 加精神指令时, 改效果添加/tick 逻辑时, 战斗引擎钩子接线时, 特性不生效时 ｜ 依据: docstring「负责管理所有战斗技能、精神指令和状态效果的注册与执行」；cb_* 回调 20 个；src/__init__.py 将 SkillRegistry/EffectManager/SpiritCommands 引入包命名空间但未列入 __all__（from src import * 拿不到）（裁决修正） ｜ @8d460a1
- **src/user/auth.py** — JWT 认证基础库：签发与解析 access token（HS256，密钥读 EBSP_JWT_SECRET_KEY，默认 7 天过期），解析失败抛 401 ｜ 相关: 签发 JWT 时, 解析/校验 Token 时, 改 Token 过期时间或密钥配置时, 改无效凭据 401 报错时 ｜ 依据: docstring「JWT 认证模块 / 提供 JWT Token 的签发和解析功能」；仅 create_access_token/decode_access_token 两函数；fastapi import 仅用于 decode 失败抛 HTTPException(401)（I1.3 耦合点） ｜ @8d460a1
- **src/user/dependencies.py** — FastAPI 认证依赖层：HTTPBearer 取 Token 经 auth.decode 还原用户（校验软删除与 active 状态），提供 get_current_user 强制鉴权依赖 ｜ 职责: 管 Token→当前用户(ORM) 的强制鉴权依赖与 401 语义；不管 Token 签发/解析原语（auth.py）、不管密码验证与用户查询实现（repository/security） ｜ 相关: 给路由加登录鉴权时, 获取当前登录用户时, 改认证失败 401 逻辑时, 调整软删除/用户状态过滤时 ｜ 依据: docstring「提供 FastAPI 依赖项，统一管理 JWT 认证逻辑」并引「Doc 7 v2.2 §11.1 批B：get_optional_user 已随匿名回退语义整体废除」（全仓无残留） ｜ @8d460a1
- **src/user/equipment_service.py** — 装备拆装业务服务：挂载/卸载/槽位顶替（Swap-out）与机体销毁前的装备显式卸载，含槽位合法性与背包容量校验 ｜ 职责: 管装备 equip/unequip 流转、槽位校验、换装与销毁机体前的容量拦截及显式卸载；不管背包容量上限计算与资产写入（委托 InventoryService）、不执行机体表 DELETE（调用方如 MechaService 负责） ｜ 相关: 改装备挂载/卸载逻辑时, 处理槽位冲突换装时, 销毁机体前卸装备时, 改装备增减的容量拦截时 ｜ 依据: docstring「装备系统服务层…实现文档 4 和 7 的以下约束」；equip/unequip/delete_mecha_safe 三方法；容量全走 inventory_service.can_add，注释明言本模块不执行机体表 DELETE ｜ @8d460a1
- **src/user/inventory.py** — 背包与货舱服务：容量上限/占用计算、准入检查（can_add）与装备材料的批量添加及溢出判定 ｜ 职责: 管货舱容量上限（经母舰提供者）、占用格数计算、准入检查与批量 add_assets 写入；不管母舰购买/切换业务（MothershipRepository）、不提交事务（仅 flush，边界归调用方） ｜ 相关: 改背包/货舱容量判定时, 发放装备或材料时, 处理 OVERFLOW 溢出时, 调整装备/材料堆叠占格规则时 ｜ 依据: docstring「背包与货舱系统服务层…设计参考：Doc 12 背包与货舱系统设计」；占用规则「离舱装备每件1格、材料每 item_id 占1格、已装备不计」（裁决修正：材料去重键为 item_id 非 item_type） ｜ @8d460a1
- **src/user/repository.py** — 用户域数据访问层（Repository Pattern）：User/母舰/机体/编队/装备/战报的 SQLAlchemy 异步读写、行锁并发控制与软删除过滤 ｜ 相关: 改用户/母舰/机体/编队/战报的数据库读写时, 加 for_update 行锁处理并发时, 改软删除(deleted_at)过滤时, 改新用户初始化默认母舰时 ｜ 依据: docstring「用户数据访问层 (Repository Pattern) / 封装所有数据库操作」；UserRepository/MothershipRepository/UserAssetRepository 三个静态方法仓储类；引 Doc 7 v2.2 §11.5 修补（squad 激活加 user_id 过滤） ｜ @8d460a1
- **src/user/schemas.py** — 用户域 Pydantic DTO：注册/登录/Token、机体/驾驶员/装备/材料 JSONB 契约、背包状态与母舰目录等请求响应模型 ｜ 相关: 改 API 请求/响应字段时, 改 JSONB 契约(upgrades/progression/random_stats)时, 改用户名/密码校验规则时, 新增 DTO 时 ｜ 依据: docstring「用户领域 Pydantic 模型 (DTO) / 用于 API 请求/响应验证」；纯 BaseModel/Enum；引 Doc 7 v2.2 §11.4（引导位）与 Doc 8（color=min(4,len(affixes)+(2 if skill)) 词条颜色分） ｜ @8d460a1
- **src/user/security.py** — 密码安全工具库：bcrypt 哈希与校验（rounds=12），不存明文 ｜ 相关: 改密码哈希/校验时, 调 bcrypt 工作因子 rounds 时 ｜ 依据: docstring「用户安全模块 / 提供密码哈希和验证功能，使用 bcrypt 算法」；仅 import bcrypt，hash_password/verify_password 两纯函数，无框架依赖 ｜ @8d460a1
- **src/user/service.py** — 用户域业务服务：母舰购买/切换业务流（PVE 锁定/配置/拥有/前置校验，经济与章节/成就为硬编码 TODO）、新号引导（claim-starter 初始机体领取+编队防御性补齐）与机体归属校验，含 SquadNotReadyError/MechasNotOwnedError 错误载体 ｜ 职责: 管 母舰购买/切换业务校验流、claim-starter 领取（with_for_update 并发防线）、assert_mechas_owned 归属校验；不管 HTTP 错误码翻译（散布 user_api/pve_api/presentation_api 三处 API 层）与数据读写细节（repository 层） ｜ 相关: 改母舰购买/切换业务时, 领初始机体 claim-starter 时, 机体归属校验时, 排查 SquadNotReadyError 分流时 ｜ 依据: 模块 docstring「用户业务逻辑层…协调 Repository 进行复杂的跨表业务操作」；Doc 7 v2.2 §11.2/§11.4/§11.5 引用落在 Error 类与 OnboardingService；注意：assert_squad_ready 当前全仓零调用方——simulate 在 v2.4 修订后内联自建校验（presentation_api.py:199），其 docstring「供 simulate 复用」已失效（对抗审核发现，裁决 grep 确认） ｜ @8d460a1
## 4. 项目洞察 / 开发记录
- **开发日志**：docs/开发日志.md（聚合 决策 / 已知坑 / 教训）
- **领域术语表**：

---

## 不变量状态
<!-- auto:invariants -->
> 状态：➖ 要求缺失（最紧急，先修） ｜ ⚠️ 建议缺失 ｜ ✅ 已对齐 ｜ ℹ️ 信息/跳过

| 不变量 | 状态 | 摘要 |
|---|---|---|
| I1.1 | ⚠️ | CLAUDE.md 里没登记 54 个模块：_utils, assembler, auth, av_dispatcher, base, battle_bridge, bidder, calculator, conditions, cont… |
| I1.2 | ✅ | 1 个工具都能跑到：pytest |
| I1.3 | ⚠️ | 路由层直接用了数据库 ORM/engine 5 处（应该走 repository/service 封装）：src/database/session.py→sqlalchemy, src/api/pve_api.py→sqlalchemy,… |
| I1.4 | ℹ️ | 这是条流程规矩（改数据要先改 schemas.py/models.py），脚本验不了，靠流程自觉 |
| I1.5 | ⚠️(3条) | CLAUDE.md 存在；没有 AGENTS.md——若用 Gemini/Copilot 等多个 agent，建议补一份，和 CLAUDE.md 保持一致；没有 PRD/总纲文档——建议写一份 docs/00_总纲.md，用来定方向、圈范围 |
| I1.6 | ⚠️(2条) | CLAUDE.md 里少了这些章节：命令, 结构, 契约；CLAUDE.md 没指向项目地图——在顶部加一行链接到 docs/项目地图.md，方便 agent 找到项目结构 |
| I1.7 | ℹ️ | CLAUDE.md 与 AGENTS.md 不同时存在，跳过同步检查 |
| I1.8 | ℹ️ | 无 0号文档，跳过内容关键词检查 |
| I1.9 | ⚠️(2条) | 无 0号文档，跳过「说人话」检查（0号在时为必须）；CLAUDE.md 里没要求「说人话」——建议加一条：让 agent 生成的文档也面向新手、用人话说 |
| I2.1 | ℹ️ | 代码质量（重复逻辑 / 烂抽象 / 泄漏抽象）不在 naofu 范围——配 code-review + simplify 两个 skill 一起用：naofu 管「结构 / 管道债」，它们管「代码质量」 |
| I2.2 | ✅ | 静态类型检查：提前发现拼写和类型错误 |
| I2.3 | ➖ | 代码格式化：统一缩进和排版，不用手动调 |
| I2.4 | ✅ | 环境与命令：虚拟环境隔离依赖，命令别裸跑 |
| I2.5 | ✅ | 自动化测试：自动验证代码行为对不对 |
| I2.6 | ➖ | 代码规范检查：抓坏味道和潜在 bug |
| I2.7 | ➖ | 规范规则配置：自定义规则，不只默认 |
| I2.8 | ➖ | 测试覆盖率：看哪些代码还没被测到 |
| I2.9 | ➖ | 提交前检查：提交时自动跑格式化+规范+类型 |
| I2.10 | ➖ | 持续集成流程：推送/PR 时自动跑测试 |
| I2.11 | ⚠️ | 依赖漏洞扫描：查第三方库有没有已知漏洞 |
| I2.12 | ⚠️ | 覆盖率门禁：覆盖率不达标就拦住提交 |
| I2.13 | ➖ | 依赖自动更新：自动提 PR 更新过期依赖 |
| I2.14 | ➖ | 密钥泄露扫描：防 API key/密码误提交 |
| I2.15 | ⚠️ | 性能基准测试：防代码偷偷变慢 |
| I2.16 | ✅ | 代码文档注释：函数/类写说明，别人看得懂 |
| I2.17 | ✅ | 跳过测试管理：每个跳过的测试写理由，别静默堆积 |
| I2.18 | ➖ | 模块测试覆盖：每个源码模块都有对应测试（41 个模块无配对测试：skills, session, base） |
| I2.19 | ✅ | Git 版本管理：代码的后悔药——能回退改动、查每个改动哪来的、开分支试错不怕搞砸 |
| I3.1 | ⚠️ | 数据契约 81/113 集中在契约文件，其余散在 9 个文件：{'src/combat/statistics_collector.py': 3, 'src/combat/entry.py': 1, 'src/combat/engageme… |
| I3.2 | ⚠️ | 纯逻辑模块用了 web 框架 3 处（应该解耦、能脱离框架单独测）：src/user/auth.py→fastapi, src/user/dependencies.py→fastapi, src/api/errors.py→fastapi |
| I3.3 | ℹ️ | docs 里有 10 处「文件:行号」指针（行号容易过时，建议改成指向函数/类名） |
| I4.1 | ➖ | 没有开发日志——决策和踩过的坑没地方记，容易反复踩同一个坑 |
| I4.4 | ⚠️ | docs/tmp 里还有 11 个临时文件没清（交付完该删掉） |
| I4.6 | ➖ | 没有术语表——项目专有名词没地方解释，新人看不懂 |
| B0 | ℹ️ | 没检出前端栈（package.json）——边界检查只对前端+后端混合项目有效，跳过 |
<!-- /auto:invariants -->

> 各不变量含义见 SKILL.md「不变量清单」。登记类（进程型）不变量在此手标状态。

---

## 补足记录（最近一次）
- 日期 / commit：
- 本次漂移：
- 待登记：
