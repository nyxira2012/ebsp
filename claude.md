
交流原则
回答问题时，先复述要求，根据重要程度和优先级回答，如果信息不足以明确回答，先询问补充信息。如果用户思路或表达有错误，直接指出，不要提供情绪价值。

## 1. 沟通与协作准则
- **强制语言**：与用户交流时必须使用 **中文**。
- **设计导向**：在设计和探讨阶段，必须 **少写代码，多说方法**。优先进行逻辑论证和方案探讨，达成一致后再动手。
- **克制开发**：不轻易增加物理文件、类和方法。坚持“高内聚、低耦合”的同时，优先考虑复用和扩展现有结构，避免过度封装。

## 2. 编码与工程标准
- **类型注解 (Type Hints)**：强制进行类型注解。使用 `Pyright` 进行静态检查。
- **模型选择**：
    - 复杂配置/运行时快照：使用 `Pydantic`。
    - 轻量级事件/原子级状态：使用 `dataclass(frozen=True)`。
- **变量命名**：禁止使用 `rifle` 等过于具体的词词作为泛指，统一使用抽象化的术语（如 `SHOOTING`）。
- **文档 (Docstrings)**：强制使用 **Google Style Docstrings**（与 `autodoc` 兼容）。核心模块必须包含中文描述，解释业务背景（如高达/机战相关技术背景）。

## 3. 验证与测试标准
- **测试框架**：使用 `pytest`。
- **随机数控制**：逻辑测试必须使用 `mock` 机制拦截 `random.uniform` 以确保测试结果可预测。
- **模拟器先行**：重大逻辑变更或数值调整后，必须运行 `scripts/sim/` 下的模拟器进行宏观验证。

